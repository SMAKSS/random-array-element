"use strict";module.exports=function randomItemWithNoRepetition(array){let copy=Array.from(array);return function(){copy.length<1&&(copy=Array.from(array));const index=Math.floor(Math.random()*copy.length),item=copy[index];return copy.splice(index,1),item}};